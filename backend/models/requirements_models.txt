# Dependencias para Fase 4: Modelos de Forecasting
# Instalar con: pip install -r backend/models/requirements_models.txt

# Modelos
prophet>=1.1.5          # Forecasting con estacionalidad (Facebook)
xgboost>=2.0.0          # Gradient Boosting (ML)

# Machine Learning
scikit-learn>=1.3.0     # Métricas y utilidades ML

# Procesamiento de datos (ya deberían estar instaladas)
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0         # Para Parquet

# Opcional: modelos adicionales
# lightgbm>=4.0.0       # Alternativa a XGBoost (más rápido)
# catboost>=1.2.0       # Otra alternativa de Gradient Boosting
